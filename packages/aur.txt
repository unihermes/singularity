# AUR packages. One per line, # comments allowed.
# Regenerate from a known-good system with:  pacman -Qqem > packages/aur.txt
#
# NOTE: read the PKGBUILD diff yay shows you. The legitimate Zen package is
# zen-browser-bin. zen-browser-PATCHED-bin was malware.

quickshell-git

# editor. vscodium-bin is the prebuilt binary; plain `vscodium` compiles
# Electron from source and takes hours. Add vscodium-bin-marketplace if you
# want the Microsoft extension marketplace instead of Open VSX.
vscodium-bin

# Claude Code CLI
claude-code
zen-browser-bin
floorp-bin
kora-icon-theme

# cursor. -bin is the prebuilt theme; plain bibata-cursor-theme renders every
# cursor from SVG with clickgen and takes a long time.
bibata-cursor-theme-bin

# apps
spotify
claude-desktop

# Discord client mod. Read the PKGBUILD before trusting any Discord mod, same
# as the zen-browser warning above -- these packages run arbitrary code
# inside your Discord client.
equicord

# Relays libcamera into a v4l2loopback webcam, only while an app has it
# open. Needed for Discord, see install.sh.
v4l2-relayd
