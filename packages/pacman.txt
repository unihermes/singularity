# Repo (native) packages. One per line, # comments allowed.
# Regenerate from a known-good system with:  pacman -Qqen > packages/pacman.txt

# base tooling
neovim
alacritty
stow
git
openssh
man-db
unzip
wget
xdg-user-dirs
fastfetch
# alt-tab.sh's fast path to alttab-relay (see hyprland.lua/alttab-relay.cpp):
# a small, non-Qt binary to hand it one line over a local socket, instead of
# spawning a whole shell (and its own process-startup cost) to do the same.
socat

# compositor + session
# ly is a TUI greeter that reads the wayland-sessions entry hyprland ships,
# so Hyprland shows up as a choice with no extra config. If pacman cannot
# find it, check `pacman -Ss ly` -- it moved from the AUR into extra, and on
# an older mirror you may need it from the AUR instead.
ly
hyprland
hyprlock
# swaybg, not hyprpaper: hyprpaper's current release needs a rendering
# protocol this Hyprland version does not implement, so nothing appears
swaybg
# the Appearance page's wallpaper colours: a Material palette from the image
matugen
xdg-desktop-portal-hyprland
xdg-desktop-portal-gtk
# polkit agent; hyprland.lua starts its user unit
hyprpolkitagent

# qt runtime (quickshell)
qt6-wayland
qt6-declarative
qt6-svg
qt6-shadertools
qt6-5compat
qt6ct

# network
# iwd handles Wi-Fi, systemd-networkd hands out addresses, systemd-resolved
# does DNS. networkd and resolved ship with systemd, so only iwd is listed.
iwd

# audio + camera (libcamera drives the IPU6 webcam through pipewire)
pipewire
pipewire-pulse
wireplumber
pipewire-libcamera
libcamera-ipa
libcamera-tools
# virtual webcam for apps that skip pipewire (Discord); see install.sh
gst-plugin-libcamera
gst-plugins-good
linux-headers
v4l2loopback-dkms
pavucontrol

# file manager + its extras
thunar
thunar-volman
thunar-archive-plugin
gvfs
gvfs-mtp
tumbler
ffmpegthumbnailer
xarchiver

# notifications + secrets
# gnome-keyring stores app secrets (browsers, VSCodium, Discord). ly's PAM
# stack already calls pam_gnome_keyring, so it starts at login. libnotify
# provides notify-send, used by screenshot.sh and anything else that wants
# to hand swaync a notification from a script.
swaync
gnome-keyring
libnotify

# wayland utilities
wofi
wl-clipboard
# clipboard history manager: persistent history of copied text/images
cliphist
grim
slurp
hyprpicker
hypridle
hyprsunset
cava
pacman-contrib

# gtk + theming
gtk3
gtk4
nwg-look
adwaita-icon-theme
hicolor-icon-theme
gtk-update-icon-cache
# GTK3 looks for an actual theme folder named "Adwaita-dark", it does not
# derive one from gtk-application-prefer-dark-theme the way libadwaita (GTK4)
# apps do. Without this package the name in settings.ini/gsettings resolves
# to nothing and GTK3 apps -- Thunar included -- silently fall back to light
# Adwaita no matter what those files say.
gnome-themes-extra

# fonts
ttf-ubuntu-nerd
ttf-ubuntu-mono-nerd
otf-commit-mono-nerd
otf-geist-mono-nerd
ttf-cascadia-code-nerd
ttf-firacode-nerd
ttf-hack-nerd
ttf-iosevka-nerd
ttf-ibmplex-mono-nerd
ttf-jetbrains-mono-nerd
ttf-martian-mono-nerd
ttf-mononoki-nerd
ttf-sourcecodepro-nerd
ttf-space-mono-nerd
ttf-terminus-nerd
ttf-victor-mono-nerd
noto-fonts
noto-fonts-emoji

# pdf viewer
# zathura is keyboard-driven and vim-keyed, which matches the rest of the
# stack. It is useless without a rendering plugin; mupdf is the fast one.
zathura
zathura-pdf-mupdf

# apps
obsidian

# laptop
brightnessctl
upower
# BlueZ itself: the bar's Bluetooth module and flyout talk to its daemon
bluez
# bt-agent. BlueZ refuses to pair at all unless some process has registered a
# pairing agent to answer its confirmation request, and neither Quickshell nor
# a non-interactive `bluetoothctl pair` provides one -- pairing just fails with
# "No agent available for request type 2".
bluez-tools
# Drives both levers this chip actually responds to: the ACPI
# platform_profile the firmware exposes, and intel_pstate's EPP. The bar's
# battery flyout switches profiles over its DBus interface.
power-profiles-daemon

# shell prompt
starship
